language: java
jdk:
  - openjdk8
  - oraclejdk8

sudo: false
install: true

branches:
    only:
    - travisLab

script:
  - ./gradlew war

after_success:
  - mkdir codedeploy
  - zip -r csye6225-web-app-${TRAVIS_JOB_NUMBER}.zip build/libs/ROOT.war infrastructrue/aws/codedeploy/appspec.yml
  - ls -al
  - mv csye6225-web-app-$TRAVIS_JOB_NUMBER.zip codedeploy/
  - ls -al
  - pwd
  - cd codedeploy
  - ls -al
  - pwd
  - cd..

notifications:
  email:
    recipients:
      - guo.bei@husky.neu.edu
      - zhu.zheny@husky.neu.edu
      - zhou.yiy@husky.neu.edu
    on_success: always
    on_failure: never
